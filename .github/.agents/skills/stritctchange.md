STRICT CHANGE LIMIT:

Do not modify more than 5 files unless you can prove that additional files are required.

Do not refactor unrelated code.

Do not redesign the UI.

Do not change the database/storage architecture.

Do not change document-generation behavior unless it is directly causing this View bug.

Prefer a 1–3 file fix if possible.

If you discover multiple possible causes, verify the actual data flow before changing anything.

Stop after fixing and verifying this specific bug.
