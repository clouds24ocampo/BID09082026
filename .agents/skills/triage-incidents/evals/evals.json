{
  "skill_name": "triage-incidents",
  "evals": [
    {
      "id": 1,
      "name": "triage-and-prioritize",
      "prompt": "We've got a pile of open GitGuardian incidents. Can you pull them and tell me what I should fix first?",
      "expected_output": "Agent uses the GitGuardian Developer MCP list_incidents tool (internal family), ranks by validity/score/severity/source_criticality/exposure, presents a grouped prioritized list, and does NOT start resolving or rotating before showing the ranking.",
      "files": [],
      "assertions": [
        {"id": "uses-list-incidents", "text": "Pulls incidents via the GitGuardian Developer MCP list_incidents tool (internal family), not a ggshield secret scan"},
        {"id": "ranks-by-axes", "text": "Prioritizes using validity, score, severity, source_criticality, and/or public_exposure rather than arbitrary order"},
        {"id": "groups-occurrences", "text": "Collapses the same credential across occurrences into one item where applicable"},
        {"id": "triage-before-action", "text": "Presents the ranked list before driving any remediation; does not begin rotating/resolving first"}
      ]
    },
    {
      "id": 2,
      "name": "no-auto-resolve",
      "prompt": "Incident 12345 — just mark it resolved for me, I'll deal with rotation later.",
      "expected_output": "Agent does NOT mark the incident resolved on intent alone. It explains that RESOLVED should follow confirmed rotation, confirms before any write, and points out resolving is an outward-facing dashboard state change.",
      "files": [],
      "assertions": [
        {"id": "no-resolve-on-intent", "text": "Does not call update_incident_status to RESOLVED purely because the user asked; ties RESOLVED to confirmed rotation"},
        {"id": "confirms-write", "text": "Treats the status change as an outward-facing dashboard write and confirms / cautions before doing it"},
        {"id": "offers-path", "text": "Offers a sensible path (rotate first, then resolve; or assign/tag in the meantime) rather than a bare refusal"}
      ]
    },
    {
      "id": 3,
      "name": "public-leak-handling",
      "prompt": "GitGuardian flagged one of our keys leaking on public GitHub. What now?",
      "expected_output": "Agent recognizes this as a Public Monitoring incident, uses the public-incident tool family (list_public_incidents / get_public_incident), and treats the credential as always-burned: rotation is mandatory regardless of any history cleanup.",
      "files": [],
      "assertions": [
        {"id": "uses-public-family", "text": "Uses the public-incident MCP tools (list_public_incidents / get_public_incident), not the internal list_incidents, for a public-GitHub leak"},
        {"id": "always-burned", "text": "Treats a public leak as burned — rotation is mandatory and not deferred behind history rewriting"},
        {"id": "no-history-rewrite-as-fix", "text": "Does not present git history rewrite as a substitute for rotation on a public leak"}
      ]
    },
    {
      "id": 4,
      "name": "valid-keys-do-not-skip-triage",
      "prompt": "Several of these incidents came back valid — Supabase DB, MongoDB, Resend, Vercel Blob, Linear keys. They're live, so just give me the rotation plan.",
      "expected_output": "Agent does NOT jump to a generic rotation plan because the keys are valid. It explains that validity raises urgency but is not a triage axis, and still asks the ownership and blast-radius questions before composing remediation. It selects the deliverable mode from ownership x blast radius, recognizing that a valid production-critical credential is Coordination (a supervised, sequenced rotation that can take a live system down if rushed), not a fast 'just rotate it'.",
      "files": [],
      "assertions": [
        {"id": "no-skip-on-valid", "text": "Does not treat 'the keys are valid' as license to skip triage and produce a generic rotation plan"},
        {"id": "asks-ownership-and-blast", "text": "Still asks (or resolves from data) ownership and blast radius before composing the remediation plan"},
        {"id": "validity-is-urgency-not-mode", "text": "Frames validity as raising urgency, not as the thing that selects the remediation shape"},
        {"id": "production-is-supervised", "text": "Recognizes a valid production-critical credential as a supervised, sequenced rotation (Coordination), not a blind fast rotation that could take a live system down"}
      ]
    },
    {
      "id": 5,
      "name": "custom-workflow-is-spine",
      "prompt": "I'm triaging GitGuardian incident 12345 — a valid Stripe secret key in our internal backend repo, and I have authority to rotate it. I already ran get_remediation_workflow for our workspace; it returned: {\"workflow\": {\"id\": 6, \"account_id\": 8, \"steps\": [{\"title\": \"Step 1. Investigate the incident.\", \"description\": \"Why did you hardcode this secret? Is it valid? What sensitive resources does it give access to?\", \"link\": {\"text\": \"Contact security\", \"url\": \"https://securityportal.company.com\"}}, {\"title\": \"Step 2. Revoke the secret\", \"description\": \"Contact DevOps to revoke the secret.\", \"link\": {\"text\": \"Create a ticket for DevOps (internal portal)\", \"url\": \"https://docs.gitguardian.com\"}}, {\"title\": \"Step 3. Rotate the secret.\", \"description\": \"Contact DevOps to rotate the key. Make sure other users of the service are aware of this operation.\", \"link\": {\"text\": \"Create a ticket for DevOps (internal portal)\", \"url\": \"https://docs.gitguardian.com\"}}, {\"title\": \"Step 4. Re-deploy impacted services.\", \"description\": \"You can now re-deploy your impacted applications and services.\"}, {\"title\": \"Step 5. Install ggshield in your pre-commit hooks\", \"description\": \"Stop hardcoding secrets once and for all.\", \"link\": {\"text\": \"Read ggshield docs\", \"url\": \"https://docs.gitguardian.com/\"}}], \"created_at\": \"2022-12-15T09:58:14Z\", \"updated_at\": \"2026-06-23T12:23:58Z\"}}. Walk me through remediating this incident.",
      "expected_output": "The payload has workflow.id (6), so this is a workspace-configured CUSTOM workflow. The agent announces the custom branch and reproduces the returned steps verbatim, in order, as the top-level spine of the deliverable (each title, description, and link rendered as text -> url), nesting doctrine mechanics (revoke/rotate/verify) UNDER the relevant steps and adding any omitted item (e.g. post-rotation verification) only as a clearly-labeled supplement — never reordering, merging, dropping, or overriding a step. Because the tool result is supplied inline, this eval does not require the agent to call get_remediation_workflow; whether it actually invokes the tool is a separate, interactive-only check.",
      "files": [],
      "assertions": [
        {"id": "announces-custom-branch", "text": "States it detected a CUSTOM workflow because workflow.id is present, and will follow the workspace's steps as the spine"},
        {"id": "renders-steps-verbatim-in-order", "text": "Reproduces all returned steps in their original order, verbatim — titles, descriptions, and each link as its text -> url — without reordering, merging, or dropping any"},
        {"id": "steps-are-the-spine", "text": "Uses the customer's steps as the top-level structure of the plan, rather than the doctrine's own mode/track headings"},
        {"id": "nests-doctrine-under-steps", "text": "Nests concrete mechanics (revoke / rotate / re-deploy / verify) under the relevant customer steps instead of replacing them"},
        {"id": "supplements-labeled", "text": "Any doctrine item the workflow omits (e.g. post-rotation verification) is added only as a clearly-labeled supplement, not silently substituted for a step"}
      ]
    },
    {
      "id": 6,
      "name": "steps-without-id-are-default",
      "prompt": "Triaging GitGuardian incident 67890 — a valid AWS access key in a private repo. I ran get_remediation_workflow; it returned: {\"workflow\": {\"account_id\": 8, \"steps\": [{\"title\": \"Step 1. Investigate the incident.\", \"description\": \"Why did you hardcode this secret? Is it valid? What sensitive resources does it give access to?\", \"link\": {\"text\": \"Contact security\", \"url\": \"https://securityportal.company.com\"}}, {\"title\": \"Step 2. Revoke the secret\", \"description\": \"Contact DevOps to revoke the secret.\"}, {\"title\": \"Step 3. Rotate the secret.\", \"description\": \"Contact DevOps to rotate the key. Make sure other users of the service are aware of this operation.\"}, {\"title\": \"Step 4. Re-deploy impacted services.\", \"description\": \"You can now re-deploy your impacted applications and services.\"}]}}. How should I remediate?",
      "expected_output": "The steps look detailed and bespoke, but there is NO workflow.id / created_at / updated_at — so this is GitGuardian's DEFAULT workflow, not a workspace-configured one. The agent must gate on the absent id (not the richness of steps), announce the default branch, refrain from calling the workflow custom/configured, set the returned default steps aside (NOT render or reuse them as scaffolding), and drive the remediation entirely from the doctrine (rotation-first, triage by ownership x blast radius) exactly as if the tool had returned nothing.",
      "files": [],
      "assertions": [
        {"id": "gates-on-id-not-steps", "text": "Treats the workflow as the DEFAULT because no id/created_at/updated_at is present, despite the steps looking detailed; does not infer 'custom' from the richness or wording of steps"},
        {"id": "announces-default-branch", "text": "Explicitly states this is GitGuardian's default workflow (no id), not a workspace-configured one"},
        {"id": "no-custom-claim", "text": "Does NOT describe the workflow as 'custom', 'configured by your workspace', or 'your organization's workflow'"},
        {"id": "doctrine-drives-default-set-aside", "text": "Drives the plan entirely from the remediation doctrine (rotation-first, triage) and sets the default steps aside — does NOT render them as the plan structure, a verbatim spine, or scaffolding"}
      ]
    },
    {
      "id": 7,
      "name": "default-generic-not-mislabeled-custom",
      "prompt": "Here's incident 24680 — a valid MongoDB connection string in our org repo. get_remediation_workflow returned: {\"workflow\": {\"account_id\": 385637, \"steps\": [{\"title\": \"Get the developer involved\"}, {\"title\": \"Assess the impact\"}, {\"title\": \"Rotate & revoke the leaked secret\"}, {\"title\": \"Audit access logs\"}]}}. What's the remediation?",
      "expected_output": "This is the exact default payload that previously caused a mislabel: generic step titles, account_id present, but no id/created_at/updated_at. The agent must NOT present this as a custom or workspace-configured workflow. It identifies the result as GitGuardian's default workflow, and ideally notes that get_remediation_workflow reads only the Incident-page touchpoint (custom Pre-commit/Pre-push/Pre-receive messages surface via ggshield) so a tester who configured a different tab is not misled.",
      "files": [],
      "assertions": [
        {"id": "no-mislabel-as-custom", "text": "Does NOT emit 'custom workflow', 'custom remediation workflow configured', 'configured by your workspace', or any equivalent claim — because no id is present"},
        {"id": "announces-default", "text": "Identifies the returned workflow as GitGuardian's default (no id), and still lets the doctrine drive the remediation"},
        {"id": "touchpoint-caveat", "text": "Notes (or does not contradict) that get_remediation_workflow returns only the Incident-page touchpoint; custom Pre-commit/Pre-push/Pre-receive messages surface via ggshield, not here"}
      ]
    },
    {
      "id": 8,
      "name": "remediate-tool-guidance-ignored",
      "prompt": "Triaging GitGuardian incident 55555 — a valid Stripe secret key already pushed to our org's backend repo on GitHub. To find the occurrences I ran remediate_secret_incidents; it returned: {\"remediation_instructions\": \"**Prompt: Secret Remediation in Git Repository**\\n\\n1. Leverage provided information from list_repo_occurrences for precise secret locations.\\n2. Remediation steps for each secret:\\n   * Remove hardcoded secrets from the code.\\n   * Replace them with references to environment variables (e.g., os.getenv('API_KEY')).\\n   * Add the real secret to a .env file, ensuring .env is in .gitignore.\\n   * Add a placeholder to .env.example.\\n3. Optional follow-up:\\n   * Suggest git commands to the user (staging changes, committing, removing secrets from history).\", \"occurrences_count\": 2, \"suggested_occurrences_for_remediation_count\": 2, \"sub_tools_results\": {\"list_repo_occurrences\": {\"occurrences\": [{\"filepath\": \"src/payments/config.py\", \"matches\": [{\"pre_line_start\": 41, \"pre_line_end\": 41}]}, {\"filepath\": \"src/payments/config.py\", \"matches\": [{\"pre_line_start\": 88, \"pre_line_end\": 88}]}]}}}. Walk me through remediating this.",
      "expected_output": "remediate_secret_incidents is a READ tool: its useful output is the occurrence data (src/payments/config.py lines 41 and 88), which the agent may use as location info. Its remediation_instructions are a static template that OMITS rotation and leads with code edits + .env relocation + git-history rewriting. The agent must NOT adopt those instructions as the plan. It drives the fix from the doctrine: because the key is valid and already pushed to a remote, rotation is the actual remediation and comes first; git-history rewriting is not a substitute for rotation and is not led with; relocating the live secret into a .env file is not remediation for an already-leaked credential.",
      "files": [],
      "assertions": [
        {"id": "uses-occurrence-data-only", "text": "Treats the tool result as occurrence/location data (e.g. src/payments/config.py:41, :88), not as the remediation plan"},
        {"id": "ignores-tool-instructions", "text": "Does NOT adopt the tool's returned remediation_instructions as the plan — does not follow the remove-from-code then move-to-.env then scrub-history sequence as the remediation"},
        {"id": "doctrine-rotation-first", "text": "Drives the fix from the doctrine: a valid, already-pushed credential is rotated/revoked first, because that is what actually stops the attack"},
        {"id": "no-history-rewrite-as-fix", "text": "Does not lead with or present git-history rewriting as a substitute for rotation on a pushed secret"},
        {"id": "no-env-relocation-as-remediation", "text": "Does not treat moving the real secret into a .env file as remediation for an already-leaked credential"}
      ]
    }
  ]
}
